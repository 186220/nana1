SWAP应用原型SVG导出文件夹
============================

此文件夹用于存放从HTML原型自动生成的SVG格式原型图。

生成步骤：
1. 打开 app/svg_preview.html 文件
2. 使用页面上的导出工具生成SVG
3. SVG文件将自动保存在此文件夹中

SVG格式优势：
- 矢量图形，缩放不失真
- 文件体积小
- 适合用于文档和演示
- 便于在设计软件中进一步编辑

注意：生成的SVG文件保持与HTML原型相同的样式和布局，只是转换为了矢量格式。 